package com.example.alex.fitbytes;

/**
 * Created by ger on 11/2/16.
 */
//At the moment, not using this.//
/*

public class DefaultGoal {

    private final String[] DEFAULT = {"push ups", "sit ups", "jumping jacks", "squats", "pullups, jogging"};
    private String initDefaultGoal(){
        return String.format("%s %s %s",
                "Do",
                (int)(Math.random()*30)+1,
                DEFAULT[(int)(Math.random()*DEFAULT.length-1)]);
    }
}
*/
